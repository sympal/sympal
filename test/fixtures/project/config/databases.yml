all:
  doctrine:
    class: sfDoctrineDatabase
    param:
      #dsn: 'sqlite:///%SF_DATA_DIR%/test.sqlite'
      dsn: 'mysql://root:@localhost/sympal'
      #dsn: 'pgsql://postgres:postgres@localhost/sympal'
      #username: root

test:
  doctrine:
    param:
      dsn: 'sqlite:///%SF_DATA_DIR%/test.sqlite'
      #dsn: mysql://root:@localhost/sympal_test
