all:
  sympal_config:
    admin_modules:
      sympal_users: ~
      sympal_groups: ~
      sympal_permissions: ~

  # if sfReCaptchaFormPlugin is present, recaptcha will be added to it
  recaptcha:
    forms:
      sfGuardRegisterForm:   true